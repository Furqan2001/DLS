export enum URLS {
  dashboard = "/dashboard",
}

export enum ROLES {
  admin = "admin",
  moderator = "moderator",
  visitor = "visitor",
}

export enum LOCAL_STORAGE_KEYS {
  web3AccountAddress = "web3AccountAddress",
}
